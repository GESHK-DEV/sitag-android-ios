import AnimalLayout from '../Components/animal-layout';
import { ScrollView } from 'react-native';

export default function Animals() {
  return (
      <ScrollView>
        <AnimalLayout />
      </ScrollView>
  );
}